[ -> 
    and := [a b -> 
        if [a] then 
            if [b] then 
                true 
            else false
        else false
    ]

    or := [a b ->
        if a then 
            true 
        else 
            if b then 
                true 
            else
                false
    ]

    printArgs := [args ->
        args ~> :null [a e -> [print e]]
    ]

    [ f -> 
        if f = "and" then and
        else if f = "or" then or
        else if f = "printArgs" then printArgs
        else { :noDeclError "Requested binding " + f + " not found in core" }
    ]
]